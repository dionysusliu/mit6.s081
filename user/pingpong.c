#include "kernel/types.h"
#include "user/user.h"

int main(int argc, char *argv[])
{ 
    // create a pipe

    // fork a child

    // child sent bytes to pipe

    // parent recv bytes from pipe

    // delete the pipe
}